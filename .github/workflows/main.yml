name: Run Tests

on: [push, pull_request]

jobs:
  lint:
    name: Run ESLint
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - run: npm install --legacy-peer-deps
      - run: npm run lint
